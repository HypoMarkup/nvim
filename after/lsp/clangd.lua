-- Override/extend nvim-lspconfig's default clangd config (lsp/clangd.lua).
-- Loaded automatically by vim.lsp when the "clangd" config is enabled.
return {
--   cmd = {
--     "clangd",
--     "--background-index",
--     "--clang-tidy",
--     "--header-insertion=iwyu",
--     "--completion-style=detailed",
--     "--function-arg-placeholders",
--     "--fallback-style=llvm",
--   },
}
